export const defaultErc20TransferAbi = `[ "function transfer(address recipient, uint256 amount) external returns (bool)"
]`;

export const defaultErc20TransferFromAbi = `["function transferFrom(address sender, address recipient, uint256 amount) external returns (bool)"
]`;
